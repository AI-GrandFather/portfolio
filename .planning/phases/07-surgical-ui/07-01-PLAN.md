---
phase: "07-surgical-ui"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["app/globals.css", "app/ui/chat-board.tsx"]
autonomous: true
requirements: ["UIUX-02", "VER-05", "VER-06"]
must_haves:
  truths:
    - "No element overlaps or overflows exist on mobile (375px) or desktop (1440px)."
    - "Container widths are consistent and centered across all sections."
    - "The chat assistant input and button are fully contained within the popup."
  artifacts:
    - path: "app/globals.css"
      provides: "Layout integrity and overflow fixes"
    - path: "app/ui/chat-board.tsx"
      provides: "Clean structure for the chat popup"
  key_links:
    - from: "app/globals.css"
      to: "main sections"
      via: ".section and .section-heading classes"
---

<objective>
Eliminate all element overlaps, overflows, and container alignment issues to achieve "Agency-of-One" precision.

Purpose: Fix critical UI bugs identified in recent screenshots and ensure perfect responsiveness.
Output: Hardened layout foundation and overflow-free components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/globals.css
@app/ui/chat-board.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global Alignment and Container Hardening</name>
  <files>app/globals.css</files>
  <action>
    - Ensure `.section` has `width: 100%`, `max-width: 1440px`, and `margin: 0 auto`.
    - Audit `.section` padding to ensure it remains within viewport bounds at all sizes.
    - Fix the `.contact-section` overlap by ensuring `.section-heading` has sufficient margin-bottom and the form doesn't have absolute positioning.
    - Add `.field-pair` class to `globals.css` (currently missing but used in `contact-form.tsx`) with `display: flex; gap: 24px;` for desktop and `flex-direction: column;` for mobile.
    - Normalize all container widths to prevent horizontal scroll.
  </action>
  <verify>
    <automated>grep -n ".field-pair" app/globals.css && grep -n "max-width: 1440px" app/globals.css</automated>
  </verify>
  <done>Global alignment issues resolved, no horizontal scroll, contact form overlap fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Chat Assistant Overflow Fix</name>
  <files>app/globals.css, app/ui/chat-board.tsx</files>
  <action>
    - In `app/globals.css`, add styles for `.chat-body` to ensure it is a flex container that fills space.
    - Update `.chat-footer` to ensure the input uses `min-width: 0` and flex-grow correctly.
    - Ensure `chat-popup` has `display: flex; flex-direction: column;` and `overflow: hidden;`.
    - In `app/ui/chat-board.tsx`, verify the structure: `chat-popup` > `chat-header` + `chat-body` > `chat-log` + `quick-prompts` + `chat-footer`. Ensure the form/footer is outside the log/prompts container if needed to prevent scrolling it out of view.
  </action>
  <verify>
    <automated>grep -n ".chat-body" app/globals.css && grep -n "min-width: 0" app/globals.css</automated>
  </verify>
  <done>Chat input and button no longer overflow their container.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client UI | Visual integrity is the primary trust signal for "Agency-of-One" positioning. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | globals.css | mitigate | Use logical CSS variables and avoid negative margins. |
| T-07-02 | Denial of Service | chat-popup | mitigate | Ensure overflow handling prevents UI-crashing layouts. |
</threat_model>

<verification>
Check site at 375px, 768px, and 1440px using browser dev tools.
Verify contact heading is clearly above form.
Verify chat footer is fully visible and contained.
</verification>

<success_criteria>
- Zero horizontal scroll at all widths.
- No text or box overlaps.
- Chat UI is perfectly contained.
</success_criteria>

<output>
After completion, create `.planning/phases/07-surgical-ui/07-01-SUMMARY.md`
</output>
