---
phase: "07-surgical-ui"
plan: "03"
type: "execute"
wave: 3
depends_on: ["07-01", "07-02"]
files_modified: []
autonomous: true
requirements: ["VER-01", "VER-02", "VER-03", "VER-05", "VER-06"]
must_haves:
  truths:
    - "The production build completes without errors."
    - "No lint or typecheck regressions are introduced."
  artifacts: []
---

<objective>
Verify the surgical UI changes against quality gates and ensure production readiness.

Purpose: Guarantee that the layout fixes and narrative restructuring did not break existing functionality.
Output: Validated, production-ready portfolio build.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-surgical-ui/07-01-SUMMARY.md
@.planning/phases/07-surgical-ui/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Quality Gates</name>
  <files></files>
  <action>
    - Run `npm run lint` to ensure no linting issues.
    - Run `npm run typecheck` to ensure no TypeScript errors (especially in `page.tsx` mapping).
    - Run `npm run build` to verify production bundling.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build</automated>
  </verify>
  <done>All quality gates passed; build is successful.</done>
</task>

</tasks>

<verification>
Review the build output for any warnings.
</verification>

<success_criteria>
- Clean lint report.
- Clean typecheck report.
- Successful production build.
</success_criteria>

<output>
After completion, create `.planning/phases/07-surgical-ui/07-03-SUMMARY.md`
</output>
