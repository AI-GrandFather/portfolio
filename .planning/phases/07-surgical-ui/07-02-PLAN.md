---
phase: "07-surgical-ui"
plan: "02"
type: "execute"
wave: 2
depends_on: ["07-01"]
files_modified: ["app/page.tsx", "app/ui/contact-form.tsx", "app/globals.css"]
autonomous: true
requirements: ["HOME-03", "UIUX-05", "VER-05"]
must_haves:
  truths:
    - "The intro section is a visual 3-column grid rather than a wall of text."
    - "Contact button text is changed to 'Submit Query'."
    - "Visual manifesto uses appropriate icons or signals for each technical pillar."
  artifacts:
    - path: "app/page.tsx"
      provides: "New visual manifesto structure"
    - path: "app/ui/contact-form.tsx"
      provides: "Updated CTA text"
    - path: "app/globals.css"
      provides: "Manifesto grid styling"
  key_links:
    - from: "app/page.tsx"
      to: "app/lib/content.ts"
      via: "BIO_FACTS.experience data mapping"
---

<objective>
Restructure the text-heavy intro into a visual "Technical Manifesto" and update key CTAs for improved conversion.

Purpose: Shift from passive reading to active visual storytelling of Mian's technical pillars.
Output: High-impact 3-column manifesto and updated conversion paths.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/page.tsx
@app/ui/contact-form.tsx
@app/lib/content.ts
# Depends on layout foundation from 01
@.planning/phases/07-surgical-ui/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Intro Section in Page.tsx</name>
  <files>app/page.tsx</files>
  <action>
    - Replace the single paragraph in `.intro-band` with a structured grid.
    - Map over `BIO_FACTS.experience` to create three feature boxes.
    - Each box should include:
      - A visual signal (simple SVG icon or symbol).
      - The technical pillar name (e.g., "AI-Native Development").
      - The description text.
    - Wrap the grid in a new class `.manifesto-grid`.
  </action>
  <verify>
    <automated>grep -n "manifesto-grid" app/page.tsx && grep -n "BIO_FACTS.experience.map" app/page.tsx</automated>
  </verify>
  <done>Intro section restructured into a 3-column grid.</done>
</task>

<task type="auto">
  <name>Task 2: Manifesto Styling and CTA Update</name>
  <files>app/globals.css, app/ui/contact-form.tsx</files>
  <action>
    - In `app/globals.css`, add styling for `.manifesto-grid`:
      - `display: grid; grid-template-columns: repeat(3, 1fr); gap: 48px;` for desktop.
      - Mobile: stack columns.
      - Add styling for manifesto items (borders, padding, typography).
      - Add a subtle hover effect to boxes for "Agency-of-One" polish.
    - In `app/ui/contact-form.tsx`, change the submit button text from "Start Conversation" to "Submit Query" (per user request).
  </action>
  <verify>
    <automated>grep -n "Submit Query" app/ui/contact-form.tsx && grep -n ".manifesto-grid" app/globals.css</automated>
  </verify>
  <done>Manifesto styled with precision; contact button text updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Content Representation | Using approved facts for the manifesto maintains truth boundary (CONT-01). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | page.tsx | mitigate | Only use public-safe facts from BIO_FACTS. |
</threat_model>

<verification>
Inspect the new manifesto grid on mobile and desktop.
Confirm contact button text is "Submit Query".
Verify SVG icons or symbols render correctly.
</verification>

<success_criteria>
- 3-column grid replaces text wall in intro.
- Professional, visual technical story is told.
- CTA is updated to requested text.
</success_criteria>

<output>
After completion, create `.planning/phases/07-surgical-ui/07-02-SUMMARY.md`
</output>
